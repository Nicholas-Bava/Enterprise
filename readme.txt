To run:

From the project directory "Enterprise"

mvn clean compile

mvn exec:java 

One the application is running, navigate in your browser to localhost:8088/person (or click the hyperlink to this location in the System output)